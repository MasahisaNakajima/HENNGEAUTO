# HENNGE証明書紐づけ自動化 要件定義書

## 1. 背景・目的

HENNGE上で発行・管理される端末証明書を、SMSM上の対象端末に紐づける作業は、現状では手作業で実施されている。  
この作業を自動化することで、作業時間の削減、作業の標準化、再実行時の負荷軽減を図る。

本件では、以下を自動化対象とする。
- HENNGEから証明書を取得する
- 取得した証明書ファイル名をIMEIに変更する
- SMSMに証明書をアップロードする
- SMSM上で対象端末を検索し、IMEIを端末に紐づける

## 2. 対象範囲

### 対象業務
- HENNGE上でユーザーを検索し、証明書をダウンロードする
- ダウンロードした証明書ファイルをIMEI名に変更する
- SMSM上で証明書をアップロードする
- SMSM上で端末を検索し、証明書を紐づける

### 非対象
- HENNGE上での証明書発行自体
- Excelの作業管理シートの作成業務
- 既存の業務フローそのものの変更

## 3. 前提条件

- 実行環境：Windows 11
- ブラウザ：Microsoft Edge
- 実行方式：自動実行（UI操作なし）
- 実装言語：Python
- 共有リンク経由でExcelを取得する
- 実行フォルダ配下に logs / screenshots を作成して保存する

## 4. 入力データ

対象となるExcelは、共有リンク経由で参照する。  
使用するシートおよび列は以下のとおりとする。

- ファイル名：HENNGE登録作業必要情報.xlsm
- シート名：HENNGE登録作業必要情報
- 読み込む列
  - C列：エイリアス（HENNGE検索用）
  - D列：シリアル番号（SMSM検索用）
  - E列：IMEI（ファイル名および紐づけ用）

### IMEIの取り扱い
- E列のIMEIをそのまま使用する
- ただし、数字の間に空白が入っている場合は、空白を削除して使用する

## 5. 業務フロー

1. Excelの対象行を読み込む
2. HENNGEにログインする
3. C列のエイリアスを使用してユーザーを検索する
4. 証明書をダウンロードする
5. ダウンロードしたファイル名をIMEIに変更する
6. SMSMにログインする
7. 証明書をアップロードする
8. D列のシリアル番号を使用して対象端末を検索する
9. 端末設定画面で、IMEIを紐づける
10. 処理結果をログとして出力する

## 6. 機能要件

### 6.1 Excel読み込み
- 共有リンク経由のExcelを読み込めること
- 1件ずつ順番に処理できること
- 指定シート・指定列のみを読み取ること

### 6.2 HENNGE操作
- HENNGEにログインできること
- ユーザー検索ができること
- 証明書のダウンロードができること

### 6.3 ファイル操作
- ダウンロードした証明書を認識できること
- IMEI名に変更できること
- 既存同名ファイルが存在する場合は、削除して再作成できること

### 6.4 SMSM操作
- SMSMにログインできること
- 証明書をアップロードできること
- 既存同名の証明書が存在する場合は、削除してからアップロードできること
- シリアル番号を使用して端末を検索できること
- 端末にIMEIを紐づけられること

### 6.5 例外処理
- エラーが発生しても処理を中断せず、次の対象へスキップして継続できること
- エラー理由をログに記録できること
- 必要に応じてスクリーンショットを保存できること

## 7. 非機能要件

- Windows 11 / Edge環境で安定動作すること
- 実行開始後は自動で処理が開始されること
- ユーザーが別の操作をしなくても処理を進められること
- 実行結果を追跡できるようにログを出力できること
- 最終的に実行ファイル（EXE）として提供できること

## 8. ログ・出力要件

- 実行フォルダ配下に logs フォルダを作成する
- 実行フォルダ配下に screenshots フォルダを作成する
- エラー時はログファイルを作成する
  - 形式例：Error_YYYYMMDD_HHMMSS.log
- 失敗箇所が分かるように、対象行・対象ユーザー・エラー内容を記録する

## 9. 認証情報の管理

- HENNGEの管理アカウントは固定で扱う
- SMSMは汎用利用を想定し、設定ファイルで管理する
- 設定ファイル形式はJSON形式とする

## 10. 実装方針

- Pythonで実装する
- Web操作はEdge上で自動化する
- 実行後は自動で処理を進める
- 将来的にはEXE化して運用しやすい形にする

## 11. 期待成果

- 手作業による証明書紐づけ作業を自動化できる
- 処理対象をまとめて実行できる
- エラー時の原因追跡が容易になる
- 運用負荷を低減できる
