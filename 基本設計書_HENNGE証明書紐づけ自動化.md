# HENNGE証明書紐づけ自動化 基本設計書

## 1. 目的

本設計書では、HENNGE上の証明書を取得し、SMSM上の対象端末に紐づける一連の作業を自動化するための基本設計を定義する。

対象は、Excelで管理される対象者情報を読み取り、HENNGEおよびSMSMの管理画面を自動操作することで、証明書の取得・登録・紐づけを実現する。

## 2. 適用範囲

- 対象業務：HENNGE証明書の取得およびSMSMへの紐づけ
- 対象環境：Windows 11 / Microsoft Edge
- 実装方式：Python
- 実行方式：自動実行
- 最終形態：EXE化を想定

## 3. 機能概要

システムは、次の処理を順次実行する。

1. Excelから対象情報を読み込む
2. HENNGEにログインする
3. ユーザー検索を行い、証明書をダウンロードする
4. ダウンロードした証明書ファイル名をIMEIに変更する
5. SMSMにログインする
6. 証明書をアップロードする
7. SMSM上で対象端末を検索する
8. 端末にIMEIを紐づける
9. 実行結果をログおよびスクリーンショットとして保存する

## 4. システム構成

### 4.1 実行環境
- OS: Windows 11
- Browser: Microsoft Edge
- Pythonバージョン: 3.11系を想定
- 実行方法: PythonスクリプトまたはEXE化した実行ファイル

### 4.2 利用ライブラリ
- SeleniumまたはPlaywright
  - Web操作を自動化するため
- openpyxl
  - Excelファイルを読み込むため
- requests
  - 必要に応じてHTTP通信を扱うため
- pathlib / os / shutil / datetime
  - ファイル操作・ログ出力・日時管理のため

## 5. 画面・操作対象

### 5.1 HENNGE
- ログイン画面
- ユーザー検索画面
- 証明書一覧画面
- 証明書ダウンロード操作

### 5.2 SMSM
- ログイン画面
- 証明書管理画面
- 端末管理画面
- 証明書アップロード画面
- 端末設定画面

## 6. データ設計

### 6.1 入力データ
対象Excelは共有リンク経由で取得し、以下の情報を読み込む。

| 項目 | 列 | 用途 |
|---|---|---|
| エイリアス | C列 | HENNGE検索用 |
| シリアル番号 | D列 | SMSM検索用 |
| IMEI | E列 | 証明書ファイル名・紐づけ用 |

### 6.2 IMEIの正規化
- E列のIMEIを読み込む
- 数字の間に空白が含まれる場合は削除する
- 例: 35 123 456 789 → 35123456789

### 6.3 設定ファイル
SMSM用の認証情報はJSON形式の設定ファイルで管理する。

例:
```json
{
  "smsm": {
    "username": "user@example.com",
    "password": "password"
  }
}
```

HENNGEの管理アカウントは固定で扱う。

## 7. 処理設計

### 7.1 全体処理
- 処理開始時に設定ファイル・ログディレクトリ・スクリーンショットディレクトリを初期化する
- Excelから対象行を読み込む
- 1件ずつ順次処理する
- 各件でエラーが発生した場合は、ログを出力し次の対象へスキップする

### 7.2 1件ごとの処理フロー
1. 対象行の情報を取得する
2. HENNGEで対象ユーザーを検索する
3. 証明書をダウンロードする
4. ダウンロードした証明書をIMEI名に変更する
5. SMSMに証明書をアップロードする
6. 対象端末を検索する
7. 端末にIMEIを紐づける
8. 成功/失敗をログに記録する

## 8. エラー処理設計

### 8.1 エラー時の方針
- 処理を中断せず、次の対象へ進める
- エラー理由をログに記録する
- 必要に応じてスクリーンショットを保存する

### 8.2 ログ出力
- ログファイル名例：Error_YYYYMMDD_HHMMSS.log
- 保存先：実行フォルダ配下の logs

### 8.3 既存ファイル対応
- 同名ファイルが存在する場合は削除してから再作成する
- SMSM側にも同名の証明書が存在する場合は削除してからアップロードする

## 9. ファイル管理設計

### 9.1 保存先ディレクトリ
- logs/
  - 実行ログ、エラーログを保存
- screenshots/
  - エラー時のスクリーンショットを保存
- downloads/
  - ダウンロードした証明書を保存

### 9.2 ファイル命名規則
- 証明書ファイル名：IMEI値
- ログファイル名：Error_YYYYMMDD_HHMMSS.log

## 10. 実装方針

### 10.1 モジュール構成案
- main.py
  - 全体制御
- config.py
  - 設定ファイル読み込み
- excel_reader.py
  - Excel読み込み
- hennge_handler.py
  - HENNGE操作
- smsm_handler.py
  - SMSM操作
- file_handler.py
  - ファイル名変更・ファイル削除
- logger.py
  - ログ出力
- utils.py
  - 共通関数

### 10.2 実行フロー
- 起動時に設定ファイルを読み込む
- 対象Excelを取得する
- 各対象に対して処理を実行する
- 最終的に処理結果をまとめて出力する

## 11. 画面操作方針

### 11.1 HENNGE操作
- ユーザー検索時はエイリアスを使う
- 証明書ダウンロード操作を実行する
- ダウンロード後、ファイルを待機して取得する

### 11.2 SMSM操作
- ログイン後、証明書管理画面へ遷移する
- 証明書をアップロードする
- 端末管理画面でシリアル番号検索を行う
- 対象端末を開き、IMEIを登録する

## 12. セキュリティ・運用上の考慮事項

- 認証情報はコード内に直接記載しない
- SMSM用の認証情報は設定ファイルで管理する
- HENNGE管理アカウントは固定で扱う
- ログにはパスワード等の機密情報を保存しない
- 実行時にスクリーンショットに個人情報が含まれないように注意する

## 13. 開発・運用方針

- 初期段階では動作確認を優先し、段階的に安定化する
- 主要処理ごとにテスト用のサンプルデータで確認する
- 実装後は、実際の業務環境での動作確認を実施する
- その後、EXE化を行い、運用しやすい形にする
